# BUILD PLAN：《你就按吧》

## Goal

做一个荒诞的「无意义仪式」：一个按钮。按它，它说话；按得越久，它越像你自己。三层内容（荒诞/讽刺/温柔）是概率池，不是关卡。背景世界极低频路过。关闭按钮会躲鼠标。双击即开，零依赖。

**风格**：像素风（Pixel Art），按钮素材为像素画，CSS `image-rendering: pixelated` 保持锐利。

---

## Simplest Solution

单文件 `index.html`，纯 HTML + CSS + JS，全部内联。

- 静态壳：CSS 画背景和按钮，不引入任何图片。
- 说话：一个文本层，`setTimeout` + CSS 过渡做淡入淡出。
- 概率池：一次 `Math.random()` 加几个计数器，三层按权重选。
- 背景路过：动态插入一个 div，CSS 动画横穿，结束后移除。
- 声音：Web Audio API 现场合成，无音频文件。
- 记忆：localStorage 三个数字（次数 / 累计按压时间 / 首次打开时间）+ 背景事件计数。

没有构建步骤，没有框架，没有请求。

---

## Tech Decision

| 选择 | 理由 |
| --- | --- |
| 原生 HTML/CSS/JS，单文件 | 双击即开、零依赖，符合「小玩意儿优先」 |
| 无框架 | 全局状态最多几个数字，不需要 React |
| Web Audio API | 没有音频文件，文件保持单文件 |
| CSS transition / animation | 淡入淡出和横穿都是声明式的，比 JS 逐帧简单 |
| localStorage | 需求只有「记住次数和时间」，不需要数据库 |

---

## Core States

```
idle ──点击──▶ speak ──淡出──▶ idle
idle ──计时──▶ background-passing ──动画结束──▶ idle
idle ──长闲置──▶ idle-whisper（一句自嘲）──▶ idle
idle ──温柔命中──▶ gentle（全页安静，唯一一句真诚）──▶ idle
```

- `idle`：按钮就位，背景静止。
- `speak`：显示一行字，淡入停留淡出。
- `background-passing`：飞机/鸭子/雨/落叶横穿，永不打断按钮。
- `gentle`：body 加 `gentle` 类，背景事件停止，按钮暂时失语，显示温柔句，若干秒后恢复。

---

## Architecture（单文件内部结构）

```
<body>
  <main id="stage">     ── 按钮 + 角落小字
  <div id="line">       ── 说话文本层（唯一内容出口）
  <div id="world">      ── 背景路过层（动态插入精灵）
  <button id="fakeClose"> ── 会躲的假关闭按钮
```

`<script>` 内按序组织（小文件，无需模块）：
1. `CONFIG`：所有可调参数（概率、间隔、语库）集中一处。
2. `store`：localStorage 读写。
3. `audio`：click / drop 两个合成音。
4. `layer()`：概率选择函数（唯一的关键逻辑）。
5. `speak(text, style)`：显示一行字。
6. `onClick()`：计次、计时、发声、触发说话。
7. `breath()`：最小间隔 + 最大等待 + 闲置自嘲 + 欢迎回来。
8. `world()`：背景事件调度 + 四个精灵。
9. `dodge()`：假关闭按钮。

---

## 概率与呼吸（Builder 直接照抄，不必重设计）

**层选择**：每次说话前算
```
gentleChance  = min(0.003 + totalPressSec * 0.00002, 0.015)   // 慢，约几小时到上限
ironicChance  = min(0.10, totalClicks * 0.0008)                // 随次数缓升，上限 10%
r = Math.random()
r < gentleChance          → 温柔
r < gentleChance + ironic → 讽刺
else                      → 荒诞
```

**呼吸节奏**：
- 点击即说话，但两次说话至少隔 `MIN_GAP = 10s`（间隔内只发声，不说话）。
- 若超过 `MAX_IDLE = 45s` 无任何事发生，强制触发一次（优先背景路过，其次一句自嘲）。
- 背景路过有自己的调度：`next = now + rand(20s, 60s)`，每次触发后重排。

**温柔表现**：`body.gentle` → 背景静止、文本居中、按钮失去响应几秒，显示温柔句，8s 后恢复。

---

## DON'T BUILD

- 用户系统 / 登录 / 账号
- 数据库 / 后端 / 网络请求
- 排行榜 / 成就系统 / 徽章
- 设置页 / 音效开关 / 进度 UI
- 多页面 / 路由 / 框架 / 构建工具 / npm
- 图片、音频、字体文件（全部 CSS/JS 生成，用系统字体）
- 移动端适配（桌面鼠标体验优先；不专门做）
- 「以后可能需要」的任何抽象

---

## Tasks

### Task 01 — 静态壳

- **目标**：第一眼的画面。
- **输入**：IDEA.md。
- **输出**：`index.html` 有结构无逻辑——像素风深蓝纯色背景，正中央像素按钮（图片素材待替换，先用纯色占位），角落一行若隐若现的小字「有时间的话，按一下。」，无标题无导航。
- **验收**：双击打开即可看到上述画面；CSS 无外链、无图片（按钮除外）。

### Task 02 — 点击与声音

- **目标**：按下去有反应。
- **输入**：Task 01。
- **输出**：按钮按压动效；Web Audio 合成咔哒声（点击）与水低声（供后复用）；点击计数 + 累计按压时长写入 localStorage；`firstOpenTime` 持久化。
- **验收**：点按有动效有声；刷新页面次数仍在；双击文件再开，`firstOpenTime` 未变。

### Task 03 — 说话引擎

- **目标**：任何一行字能优雅地出现又离开。
- **输入**：Task 01。
- **输出**：`#line` 文本层 + `speak(text, style)`，淡入→停留→淡出；点击触发说话；**概率函数 `layer()` 实现**（参数见上表）；两次说话至少隔 10s（间隔内只发声不写字）。
- **验收**：连点 20 次不会出现两行字叠在一起；淡出后文本层清空。

### Task 04 — 内容池

- **目标**：三层语料齐全。
- **输入**：Task 03。
- **输出**：
  - 荒诞层 ≥ 8 句，含**统计彩蛋**模板（读计数器拼句，如「你已按了 537 次，耗时 10 分 21 秒，期间有 3 架飞机飞过，下过 1 次雨」）、虚惊倒计时（如读秒到 0 后「骗你的，继续按。」）、「继续按」。
  - 讽刺层 ≥ 5 句（「你在期待什么？」「你想干什么？」「你其实只是在等点什么吧。」）。
  - 温柔层：唯一定义句 + `body.gentle` 全页安静效果（背景停、按钮失语、居中，8s 后恢复）。
  - 提供开发开关 `window.DEBUG_FORCE = 'gentle' | 'irony' | 'absurd'` 强制某层，方便验收，不影响正常逻辑。
- **验收**：用 DEBUG_FORCE 依次强制三层，各自正确显示；统计句数字随实际计数变化。

### Task 05 — 呼吸与记忆

- **目标**：不轰炸、不冷场、记得你是谁。
- **输入**：Task 03。
- **输出**：`breath()` —— 45s 无事件强制触发一次（优先背景路过，其次一句自嘲）；「你回来了」逻辑（距上次打开超过阈值时，打开时显示）；点击间隔内静默。
- **验收**：挂机 1 分钟至少见到一次自动事件；连续狂点不刷屏；第二次打开显示「你回来了。」

### Task 06 — 背景世界

- **目标**：极低频、安静的路过。
- **输入**：Task 01 + Task 05 的调度。
- **输出**：`#world` 层 + 四个精灵（小飞机横穿天空一角 / 鸭子横穿底部 / 几滴雨 / 风推落叶），CSS 动画横穿后移除节点；调度间隔 20–60s 随机；事件从不拦截按钮点击；雨/飞机等计入统计计数。
- **验收**：停留 3–5 分钟能看到至少一次路过；期间按钮始终可点；事件过后背景恢复静止。

### Task 07 — 会躲的关闭按钮

- **目标**：别扭的挽留。
- **输入**：Task 01。
- **输出**：打开页面后一段时间（如 60–120s 随机）浮现一个小小的「关」按钮；鼠标靠近时悄悄挪开一点（每次挪开是「别扭」的，小步、停顿，不是瞬间瞬移）；点中时尝试 `window.close()`；被浏览器拦截则它叹了口气（如显示「好吧。」）后消失。
- **验收**：鼠标靠近多次它都能躲开；强行点中不会报错，页面行为符合描述。

---

## 完成标准

Builder 按 Task 顺序做，每步验收通过即继续。无需重新设计技术方案。全部完成后：双击打开即体验完整闭环。
